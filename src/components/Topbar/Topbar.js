import React from 'react';
import classes from './Topbar.module.scss';

const Topbar = () => {
  return (
    <div className={classes.Topbar}>
      <h2 className={classes.Logo}>Ui Design</h2>
    </div>
  );
}

export default Topbar;
